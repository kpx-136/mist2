/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./*.{html,js}" // Tells the extension to scan HTML and JS files for classes
    ], 
    theme: {
        extend: {
            
        },
    },
    plugins: [],
}